# Claude Code Notifier — backend configuration
# One backend per line: name=path
# ${SCRIPT_DIR} expands to the notifier install directory
telegram=${SCRIPT_DIR}/telegram-send.sh
# slack=${SCRIPT_DIR}/slack-send.sh
# ntfy=${SCRIPT_DIR}/ntfy-send.sh
# discord=${SCRIPT_DIR}/discord-send.sh
